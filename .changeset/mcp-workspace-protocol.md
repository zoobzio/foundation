---
"@zoobzio/foundation-mcp": patch
---

Republish with a resolved peer dependency: 0.0.14 shipped with the raw `workspace:*` protocol in `peerDependencies`, making the package uninstallable outside the monorepo.
