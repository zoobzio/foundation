---
"@zoobzio/foundation": minor
---

Add `core/tree` (reka-ui Tree wrapper with link duality and lazy-load-friendly toggle emits), `core/breadcrumb` (semantic trail with an inert `aria-current` terminal item), and the `data/browser` feature (fetch-driven file browser: alphabetical folders over a sortable file table, breadcrumb navigation, file/folder/bulk actions). Adds a shared `filesize` column type to the table's column vocabulary and `file`/`folder`/`folder-open` icon aliases. Fixes the table widget blanking the head's default sortable headers when no `header` slot was supplied.
