import type { Theme, ThemeManifestEntry, CustomColor } from "../../src/types";

export const defaultTheme: string = "";
export const manifest: ThemeManifestEntry[] = [];
export const defaultThemeData: Theme = {} as Theme;
export const colors: Record<string, CustomColor> = {};
export const roles: Record<string, string> = {};
