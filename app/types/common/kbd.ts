import type { OreColors, OreDensity, OreElevation, OreRadius, OreSizes, OreVariants } from "#foundation/types/common/modifiers";
export type KbdProps = {
  variant?: OreVariants["kbd"];
  size?: OreSizes["kbd"];
  color?: OreColors["kbd"];
  radius?: OreRadius["kbd"];
  density?: OreDensity["kbd"];
  elevation?: OreElevation["kbd"];
};
