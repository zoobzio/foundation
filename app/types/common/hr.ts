import type { OreColors, OreDensity, OreElevation, OreRadius, OreSizes, OreVariants } from "#foundation/types/common/modifiers";
export type HrProps = {
  variant?: OreVariants["hr"];
  size?: OreSizes["hr"];
  color?: OreColors["hr"];
  radius?: OreRadius["hr"];
  density?: OreDensity["hr"];
  elevation?: OreElevation["hr"];
};
